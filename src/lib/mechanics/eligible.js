/** Layer A: eligible-mod queries. Occupied-group logic lives in spawnWeights — one probability model. */
export { getEligibleMods, harvestEligiblePool, collectOccupiedGroups } from '../spawnWeights.js';
